const lessons = [
  {
    id: "casa-curtea-bunicii",
    title: "Casa și curtea bunicii",
    huTitle: "Nagymama háza és udvara",
    page: "#80",
    sourceUrl:
      "https://manuale.edu.ro/manuale/Clasa%20I/Comunicare%20in%20limba%20romana%20pentru%20scolile%20si%20sectiile%20cu%20predare%20in%20limba%20maghiara/RURJVFVSQSBDT1JWSU4g/#80",
    color: "#f97316",
    cover: "Images/Casa.png",
    coverSource: "Local",
    words: [
      { id: "perete", hu: "fal", ro: "perete", image: "Images/Perete.png", color: "#ca8a04" },
      { id: "poarta", hu: "kapu", ro: "poartă", image: "Images/Poarta.png", color: "#92400e" },
      { id: "gard", hu: "kerítés", ro: "gard", image: "Images/Gard.png", color: "#78716c" },
      { id: "acoperis", hu: "tető", ro: "acoperiș", image: "Images/Acoperis.png", color: "#f97316" },
      { id: "horn", hu: "kémény", ro: "horn", image: "Images/Horn.png", color: "#7c2d12" },
      { id: "curte", hu: "udvar", ro: "curte", image: "Images/Curte.png", color: "#22c55e" },
      { id: "gradina", hu: "kert", ro: "grădină", image: "Images/Gradina.png", color: "#db2777" },
      { id: "copac", hu: "fa", ro: "copac", image: "Images/Copac.png", color: "#16a34a" },
      { id: "banca", hu: "pad", ro: "bancă", image: "Images/Banca.png", color: "#a16207" },
      { id: "leagan", hu: "hinta", ro: "leagăn", image: "Images/Leagan.png", color: "#7c3aed" },
      { id: "terasa", hu: "terasz", ro: "terasă", image: "Images/Terasa.png", color: "#0ea5e9" },
      { id: "grajd", hu: "istálló", ro: "grajd", image: "Images/Grajd.png", color: "#dc2626" },
      { id: "fantana", hu: "kút", ro: "fântână", image: "Images/Fantana.png", color: "#0284c7" },
      { id: "gradina-flori", hu: "virágos kert", ro: "grădină cu flori", image: "Images/GradinaCuFlori.png", color: "#ec4899" },
      { id: "cutie", hu: "postaláda", ro: "cutie poștală", image: "Images/CutiePostala.png", color: "#2563eb" },
    ],
  },
  {
    id: "padurea-animalele-salbatice",
    title: "Pădurea și animalele sălbatice",
    huTitle: "Az erdő és a vadállatok",
    page: "#91",
    sourceUrl:
      "https://manuale.edu.ro/manuale/Clasa%20I/Comunicare%20in%20limba%20romana%20pentru%20scolile%20si%20sectiile%20cu%20predare%20in%20limba%20maghiara/RURJVFVSQSBDT1JWSU4g/#91",
    color: "#16a34a",
    cover: "Images/Padure.png",
    coverSource: "Local",
    words: [
      {
        id: "padure",
        hu: "erdő",
        ro: "pădure",
        image: "Images/Padure.png",
        color: "#15803d",
      },
      {
        id: "urs",
        hu: "medve",
        ro: "urs",
        image: "Images/Urs.png",
        color: "#7c2d12",
      },
      {
        id: "veverita",
        hu: "mókus",
        ro: "veveriță",
        image: "Images/Veverita.png",
        color: "#b45309",
      },
      {
        id: "lup",
        hu: "farkas",
        ro: "lup",
        image: "Images/Lup.png",
        color: "#475569",
      },
      {
        id: "vulpe",
        hu: "róka",
        ro: "vulpe",
        image: "Images/Vulpe.png",
        color: "#ea580c",
      },
      {
        id: "caprioara",
        hu: "őz",
        ro: "căprioară",
        image: "Images/Caprioara.png",
        color: "#a16207",
      },
      {
        id: "cerb",
        hu: "szarvas",
        ro: "cerb",
        image: "Images/Cerb.png",
        color: "#92400e",
      },
      {
        id: "mistret",
        hu: "vaddisznó",
        ro: "mistreț",
        image: "Images/Mistret.png",
        color: "#57534e",
      },
      {
        id: "ciocanitoare",
        hu: "harkály",
        ro: "ciocănitoare",
        image: "Images/Ciocanitoare.png",
        color: "#dc2626",
      },
      {
        id: "padurar",
        hu: "erdész",
        ro: "pădurar",
        image: "Images/Padurar.png",
        color: "#166534",
      },
      {
        id: "ghinda",
        hu: "makk",
        ro: "ghindă",
        image: "Images/Ghinda.png",
        color: "#92400e",
      },
      {
        id: "aluna",
        hu: "mogyoró",
        ro: "alună",
        image: "Images/Aluna.png",
        color: "#a16207",
      },
      {
        id: "zmeura",
        hu: "málna",
        ro: "zmeură",
        image: "Images/Zmeura.png",
        color: "#e11d48",
      },
    ],
  },
  {
    id: "sa-circulam-corect",
    title: "Să circulăm corect!",
    huTitle: "Közlekedjünk helyesen!",
    page: "#78",
    sourceUrl:
      "https://manuale.edu.ro/manuale/Clasa%20I/Comunicare%20in%20limba%20romana%20pentru%20scolile%20si%20sectiile%20cu%20predare%20in%20limba%20maghiara/RURJVFVSQSBDT1JWSU4g/#78",
    color: "#2563eb",
    cover: "Images/TrecereDePietoni.png",
    coverSource: "Local",
    words: [
      { id: "bulevard", hu: "sugárút", ro: "bulevard", image: "Images/Bulevard.png", color: "#2563eb" },
      { id: "drum", hu: "út", ro: "drum", image: "Images/Drum.png", color: "#475569" },
      { id: "sosea", hu: "országút", ro: "șosea", image: "Images/Sosea.png", color: "#64748b" },
      { id: "trotuar", hu: "járda", ro: "trotuar", image: "Images/Trotuar.png", color: "#94a3b8" },
      { id: "zebra", hu: "zebra", ro: "zebră", image: "Images/Zebra.png", color: "#111827" },
      { id: "trecere-pietoni", hu: "gyalogátkelőhely", ro: "trecere de pietoni", image: "Images/TrecereDePietoni.png", color: "#0ea5e9" },
      { id: "pieton", hu: "gyalogos", ro: "pieton", image: "Images/Pieton.png", color: "#f97316" },
      { id: "pietoni", hu: "gyalogosok", ro: "pietoni", image: "Images/Pietoni.png", color: "#fb923c" },
      { id: "semafor", hu: "közlekedési lámpa", ro: "semafor", image: "Images/Semafor.png", color: "#16a34a" },
      { id: "porneste", hu: "elindul", ro: "pornește", image: "Images/Porneste.png", color: "#22c55e" },
      { id: "se-opreste", hu: "megáll", ro: "se oprește", image: "Images/SeOpreste.png", color: "#dc2626" },
      { id: "traverseaza", hu: "átkel", ro: "traversează", image: "Images/Traverseaza.png", color: "#8b5cf6" },
      { id: "viteza", hu: "sebesség", ro: "viteză", image: "Images/Viteza.png", color: "#eab308" },
      { id: "taxi", hu: "taxi", ro: "taxi", image: "Images/Taxi.png", color: "#facc15" },
      { id: "salvare", hu: "mentőautó", ro: "salvare", image: "Images/Salvare.png", color: "#ef4444" },
      { id: "pompieri", hu: "tűzoltók", ro: "pompieri", image: "Images/Pompieri.png", color: "#b91c1c" },
      { id: "masina-pompieri", hu: "tűzoltóautó", ro: "mașină de pompieri", image: "Images/MasinaDePompieri.png", color: "#dc2626" },
      { id: "politie", hu: "rendőrség", ro: "poliție", image: "Images/Politie.png", color: "#1d4ed8" },
      { id: "masina-politie", hu: "rendőrautó", ro: "mașină de poliție", image: "Images/MasinaDePolitie.png", color: "#0f172a" },
    ],
  },
];

const app = document.querySelector("#app");
const colors = ["#ff6b6b", "#ffd166", "#22c55e", "#0ea5e9", "#a855f7", "#f472b6", "#fb923c"];
const MATCH_BATCH_SIZE = 5;

const state = {
  screen: "lessons",
  mode: null,
  selectedLessonId: lessons[0].id,
  remaining: [],
  current: null,
  options: [],
  found: 0,
  feedback: "",
  feedbackType: "",
  locked: false,
  selectedLeft: null,
  matches: new Map(),
  matchOrder: [],
  matchBatchIndex: 0,
  batchWords: [],
  leftOrder: [],
  rightOrder: [],
  drag: null,
  runId: 0,
  timer: null,
};

function currentLesson() {
  return lessons.find((lesson) => lesson.id === state.selectedLessonId) || lessons[0];
}

function currentWords() {
  return currentLesson().words;
}

function shuffle(items) {
  return [...items].sort(() => Math.random() - 0.5);
}

function sampleOptions(answer) {
  const words = currentWords();
  const choices = shuffle(words.filter((item) => item.id !== answer.id)).slice(0, 3);
  return shuffle([answer, ...choices]);
}

function selectLesson(id) {
  resetTimer();
  state.selectedLessonId = id;
  state.screen = "activities";
  state.mode = null;
  state.feedback = "";
  render();
}

function startRound(mode) {
  resetTimer();
  state.runId += 1;
  state.screen = "game";
  state.mode = mode;
  state.remaining = shuffle(currentWords());
  state.found = 0;
  state.feedback = "";
  state.feedbackType = "";
  state.locked = false;
  nextQuestion();
}

function nextQuestion() {
  if (state.mode !== "word" && state.mode !== "picture") return;

  if (!state.remaining.length) {
    celebrate();
    return;
  }

  state.current = state.remaining[0];
  state.options = sampleOptions(state.current);
  state.feedback = "";
  state.feedbackType = "";
  state.locked = false;
  render();
}

function chooseAnswer(id) {
  if (state.locked) return;

  if (id !== state.current.id) {
    state.feedback = "Mai încearcă!";
    state.feedbackType = "try";
    flashAnswer(id, "wrong");
    renderFeedback();
    return;
  }

  state.locked = true;
  state.feedback = "Corect!";
  state.feedbackType = "good";
  state.found += 1;
  state.remaining.shift();
  flashAnswer(id, "correct");
  renderFeedback();
  renderProgress();

  const runId = state.runId;
  state.timer = setTimeout(() => {
    if (state.runId !== runId) return;
    nextQuestion();
  }, 900);
}

function flashAnswer(id, className) {
  const button = document.querySelector(`[data-answer="${id}"]`);
  if (!button) return;
  button.classList.remove("wrong", "correct");
  void button.offsetWidth;
  button.classList.add(className);
}

function renderFeedback() {
  const feedback = document.querySelector(".feedback");
  if (!feedback) return;
  feedback.textContent = state.feedback;
  feedback.className = `feedback show ${state.feedbackType}`;
}

function renderProgress() {
  const words = currentWords();
  const progress = document.querySelector(".progress-fill");
  const label = document.querySelector(".progress-label");
  if (!progress || !label) return;
  progress.style.width = `${(state.found / words.length) * 100}%`;
  label.textContent = `${state.found}/${words.length}`;
}

function startMatchMode() {
  resetTimer();
  state.runId += 1;
  state.screen = "game";
  state.mode = "match";
  state.found = 0;
  state.feedback = "";
  state.feedbackType = "";
  state.selectedLeft = null;
  state.matchOrder = shuffle(currentWords());
  state.matchBatchIndex = 0;
  state.drag = null;
  loadMatchBatch();
}

function loadMatchBatch() {
  const start = state.matchBatchIndex * MATCH_BATCH_SIZE;
  state.batchWords = state.matchOrder.slice(start, start + MATCH_BATCH_SIZE);
  state.matches = new Map();
  state.selectedLeft = null;
  state.leftOrder = shuffle(state.batchWords);
  state.rightOrder = shuffle(state.batchWords);
  state.drag = null;
  render();
  requestAnimationFrame(drawMatchLines);
}

function selectLeft(id) {
  if (state.matches.has(id)) return;
  state.selectedLeft = state.selectedLeft === id ? null : id;
  drawMatchLines();
  renderMatchItems();
}

function selectRight(id) {
  const leftId = state.selectedLeft;
  if (!leftId || [...state.matches.values()].includes(id)) return;
  checkPair(leftId, id);
}

function checkPair(leftId, rightId) {
  if (leftId !== rightId) {
    state.feedback = "Mai încearcă!";
    state.feedbackType = "try";
    state.selectedLeft = null;
    renderFeedback();
    markWrong(leftId, rightId);
    return;
  }

  state.matches.set(leftId, rightId);
  state.selectedLeft = null;
  state.found += 1;
  state.feedback = "Corect!";
  state.feedbackType = "good";
  renderMatchItems();
  drawMatchLines();
  renderFeedback();
  renderProgress();

  if (state.matches.size === state.batchWords.length) {
    const runId = state.runId;
    state.timer = setTimeout(() => {
      if (state.runId !== runId || state.mode !== "match") return;
      if (state.found >= currentWords().length) {
        celebrate();
        return;
      }
      state.matchBatchIndex += 1;
      state.feedback = "";
      state.feedbackType = "";
      loadMatchBatch();
    }, 650);
  }
}

function resetTimer() {
  if (!state.timer) return;
  clearTimeout(state.timer);
  state.timer = null;
}

function goLessons() {
  resetTimer();
  state.runId += 1;
  state.screen = "lessons";
  state.mode = null;
  state.drag = null;
  render();
}

function goActivities() {
  resetTimer();
  state.runId += 1;
  state.screen = "activities";
  state.mode = null;
  state.drag = null;
  state.feedback = "";
  render();
}

function markWrong(leftId, rightId) {
  const left = document.querySelector(`[data-left="${leftId}"]`);
  const right = document.querySelector(`[data-right="${rightId}"]`);
  [left, right].forEach((item) => {
    if (!item) return;
    item.classList.remove("wrong");
    void item.offsetWidth;
    item.classList.add("wrong");
    setTimeout(() => item.classList.remove("wrong"), 420);
  });
  renderMatchItems();
  drawMatchLines();
}

function handlePointerDown(event, id) {
  if (state.screen !== "game" || state.mode !== "match" || state.matches.has(id)) return;
  const board = document.querySelector(".match-board");
  const start = getConnectorPoint(`[data-left="${id}"]`, "right", board);
  state.drag = { leftId: id, x1: start.x, y1: start.y, x2: start.x, y2: start.y };
  state.selectedLeft = id;
  event.currentTarget.setPointerCapture?.(event.pointerId);
  renderMatchItems();
  drawMatchLines();
}

function handlePointerMove(event) {
  if (!state.drag) return;
  const board = document.querySelector(".match-board");
  const rect = board.getBoundingClientRect();
  state.drag.x2 = event.clientX - rect.left;
  state.drag.y2 = event.clientY - rect.top;
  drawMatchLines();
}

function handlePointerUp(event) {
  if (!state.drag) return;
  const rightElement = document.elementFromPoint(event.clientX, event.clientY)?.closest("[data-right]");
  const leftId = state.drag.leftId;
  state.drag = null;
  if (rightElement) {
    checkPair(leftId, rightElement.dataset.right);
  } else {
    state.selectedLeft = null;
    renderMatchItems();
    drawMatchLines();
  }
}

function renderMatchItems() {
  document.querySelectorAll(".match-item").forEach((item) => {
    const leftId = item.dataset.left;
    const rightId = item.dataset.right;
    const isLeftDone = leftId && state.matches.has(leftId);
    const isRightDone = rightId && [...state.matches.values()].includes(rightId);
    const isSelected = leftId && state.selectedLeft === leftId;
    item.classList.toggle("done", Boolean(isLeftDone || isRightDone));
    item.classList.toggle("selected", Boolean(isSelected));
    item.classList.toggle("dim", Boolean(state.selectedLeft && rightId && isRightDone));
  });
}

function getConnectorPoint(selector, side, board) {
  const element = document.querySelector(selector);
  const boardRect = board.getBoundingClientRect();
  const rect = element.getBoundingClientRect();
  return {
    x: side === "right" ? rect.right - boardRect.left : rect.left - boardRect.left,
    y: rect.top + rect.height / 2 - boardRect.top,
  };
}

function drawMatchLines() {
  const board = document.querySelector(".match-board");
  const svg = document.querySelector(".match-lines");
  if (!board || !svg) return;

  const lines = [];
  state.matches.forEach((rightId, leftId) => {
    const start = getConnectorPoint(`[data-left="${leftId}"]`, "right", board);
    const end = getConnectorPoint(`[data-right="${rightId}"]`, "left", board);
    const c1 = start.x + Math.max(42, (end.x - start.x) * 0.38);
    const c2 = end.x - Math.max(42, (end.x - start.x) * 0.38);
    lines.push(`<path class="done-line" d="M ${start.x} ${start.y} C ${c1} ${start.y}, ${c2} ${end.y}, ${end.x} ${end.y}" />`);
  });

  if (state.drag) {
    lines.push(`<line class="drag-line" x1="${state.drag.x1}" y1="${state.drag.y1}" x2="${state.drag.x2}" y2="${state.drag.y2}" />`);
  }

  svg.innerHTML = lines.join("");
}

function celebrate() {
  const confetti = Array.from({ length: 90 }, (_, index) => {
    const left = Math.random() * 100;
    const delay = Math.random() * 1.4;
    const duration = 2.4 + Math.random() * 2.6;
    const color = colors[index % colors.length];
    return `<span class="confetti" style="left:${left}%;background:${color};animation-delay:${delay}s;animation-duration:${duration}s"></span>`;
  }).join("");

  app.insertAdjacentHTML(
    "beforeend",
    `<section class="celebration" role="dialog" aria-modal="true">
      ${confetti}
      <div class="celebration-message">
        <h2>BRAVO!</h2>
        <p>Ügyes vagy! - Ești un campion!</p>
        <button class="pill" data-action="activities">Játékok</button>
      </div>
    </section>`,
  );
}

function renderShell(inner) {
  const lesson = currentLesson();
  const subtitle = state.screen === "lessons" ? "Válassz leckét" : lesson.title;
  const right =
    state.screen === "lessons"
      ? `<div class="pill">${lessons.length} lecke</div>`
      : state.screen === "activities"
        ? `<button class="back-button" data-action="lessons" aria-label="Vissza">←</button>`
        : `<button class="back-button" data-action="activities" aria-label="Vissza">←</button>`;

  app.innerHTML = `
    <section class="stage">
      <header class="top-bar">
        <div class="brand">
          <div class="logo-burst">R</div>
          <div>
            <h1>Román szókaland</h1>
            <p>${subtitle}</p>
          </div>
        </div>
        ${right}
      </header>
      ${inner}
    </section>
  `;
}

function renderLessonSelect() {
  renderShell(`
    <section class="lesson-grid" aria-label="Lecke választás">
      ${lessons
        .map(
          (lesson) => `
            <button class="lesson-card" data-lesson="${lesson.id}" style="--lesson-color:${lesson.color}">
              <img class="lesson-cover" src="${lesson.cover}" alt="${lesson.huTitle}" />
              <div class="lesson-info">
                <span class="lesson-page">${lesson.page}</span>
                <h2>${lesson.title}</h2>
                <p>${lesson.words.length} szó</p>
              </div>
            </button>
          `,
        )
        .join("")}
    </section>
  `);
}

function renderActivityMenu() {
  const lesson = currentLesson();
  renderShell(`
    <section class="lesson-selected">
      <img src="${lesson.cover}" alt="${lesson.huTitle}" />
      <div>
        <a class="lesson-page source-link" href="${lesson.sourceUrl}" target="_blank" rel="noopener noreferrer">${lesson.page}</a>
        <h2>${lesson.title}</h2>
        <p>${lesson.huTitle} - ${lesson.words.length} szó</p>
      </div>
    </section>
    <section class="menu-grid" aria-label="Játékmód választás">
      <button class="menu-card word" data-mode="word">
        <div class="menu-icon">Aa</div>
        <div>
          <h2>Szó - szó</h2>
          <p>Magyarból román</p>
        </div>
      </button>
      <button class="menu-card picture" data-mode="picture">
        <div class="menu-icon">▣</div>
        <div>
          <h2>Kép - szó</h2>
          <p>Nézd meg, válaszd ki</p>
        </div>
      </button>
      <button class="menu-card match" data-mode="match">
        <div class="menu-icon">↔</div>
        <div>
          <h2>Húzd össze</h2>
          <p>Balról jobbra</p>
        </div>
      </button>
    </section>
  `);
}

function renderQuiz() {
  const words = currentWords();
  const isPicture = state.mode === "picture";
  const boardContent = isPicture
    ? pictureCard(state.current)
    : `<div>
        <p class="task-label">Keresd meg ezt:</p>
        <div class="target-chip"><h2 class="target-word">${state.current.hu}</h2></div>
      </div>`;

  renderShell(`
    <section class="game-panel">
      <div class="progress-row">
        <div class="progress-track" aria-label="Haladás"><div class="progress-fill" style="width:${(state.found / words.length) * 100}%"></div></div>
        <div class="pill progress-label">${state.found}/${words.length}</div>
      </div>
      <div class="task-board">
        ${isPicture ? '<p class="task-label">Melyik román szó illik ehhez?</p>' : ""}
        ${boardContent}
      </div>
      <div class="answers">
        ${state.options
          .map(
            (option) =>
              `<button class="answer-button" data-answer="${option.id}">
                ${option.ro}
              </button>`,
          )
          .join("")}
      </div>
      <div class="feedback ${state.feedback ? `show ${state.feedbackType}` : ""}">${state.feedback}</div>
    </section>
  `);
}

function renderMatchMode() {
  const words = currentWords();
  const batchWords = state.batchWords.length ? state.batchWords : words.slice(0, MATCH_BATCH_SIZE);
  const batchCount = Math.ceil(words.length / MATCH_BATCH_SIZE);
  renderShell(`
    <section class="game-panel">
      <div class="progress-row">
        <div class="progress-track" aria-label="Haladás"><div class="progress-fill" style="width:${(state.found / words.length) * 100}%"></div></div>
        <div class="pill progress-label">${state.found}/${words.length}</div>
        <div class="pill batch-label">Csoport ${state.matchBatchIndex + 1}/${batchCount}</div>
      </div>
      <p class="task-label">Húzd össze a magyar szót a román párjával!</p>
      <div class="match-board">
        <svg class="match-lines" aria-hidden="true"></svg>
        <div class="match-columns">
          <div class="match-column" style="--word-count:${batchWords.length}">
            ${state.leftOrder
              .map((item) => `<button class="match-item left" data-left="${item.id}">${item.hu}</button>`)
              .join("")}
          </div>
          <div class="match-column" style="--word-count:${batchWords.length}">
            ${state.rightOrder
              .map((item) => `<button class="match-item right" data-right="${item.id}">${item.ro}</button>`)
              .join("")}
          </div>
        </div>
      </div>
      <div class="feedback ${state.feedback ? `show ${state.feedbackType}` : ""}">${state.feedback}</div>
    </section>
  `);
  renderMatchItems();
  drawMatchLines();
}

function render() {
  if (state.screen === "lessons") renderLessonSelect();
  if (state.screen === "activities") renderActivityMenu();
  if (state.screen === "game" && (state.mode === "word" || state.mode === "picture")) renderQuiz();
  if (state.screen === "game" && state.mode === "match") renderMatchMode();
}

function iconUrl(item) {
  const [prefix, name] = item.icon.split(":");
  const color = encodeURIComponent(item.color);
  return `https://api.iconify.design/${prefix}/${name}.svg?color=${color}&width=240&height=240`;
}

function visualUrl(item) {
  return item.image || iconUrl(item);
}

function pictureCard(item) {
  return `
    <div class="picture-wrap" aria-label="${item.hu} illusztráció">
      <div class="picture-card ${item.image ? "photo-card" : ""}" style="--picture-color:${item.color}">
        <img class="picture-image" src="${visualUrl(item)}" alt="${item.hu}" />
        <div class="image-fallback" aria-hidden="true">${item.hu}</div>
      </div>
      <div class="picture-name">${item.hu}</div>
    </div>
  `;
}

app.addEventListener("click", (event) => {
  const lessonButton = event.target.closest("[data-lesson]");
  if (lessonButton) {
    selectLesson(lessonButton.dataset.lesson);
    return;
  }

  const modeButton = event.target.closest("[data-mode]");
  if (modeButton) {
    modeButton.dataset.mode === "match" ? startMatchMode() : startRound(modeButton.dataset.mode);
    return;
  }

  const action = event.target.closest("[data-action]");
  if (action?.dataset.action === "lessons") {
    goLessons();
    return;
  }
  if (action?.dataset.action === "activities") {
    goActivities();
    return;
  }

  const answer = event.target.closest("[data-answer]");
  if (answer) {
    chooseAnswer(answer.dataset.answer);
    return;
  }

  const left = event.target.closest("[data-left]");
  if (left) {
    selectLeft(left.dataset.left);
    return;
  }

  const right = event.target.closest("[data-right]");
  if (right) {
    selectRight(right.dataset.right);
  }
});

app.addEventListener("pointerdown", (event) => {
  const left = event.target.closest("[data-left]");
  if (!left) return;
  handlePointerDown(event, left.dataset.left);
});

app.addEventListener("pointermove", handlePointerMove);
app.addEventListener("pointerup", handlePointerUp);
app.addEventListener(
  "error",
  (event) => {
    if (!event.target.classList?.contains("picture-image") && !event.target.classList?.contains("lesson-cover")) return;
    event.target.hidden = true;
    event.target.closest(".picture-card")?.classList.add("image-missing");
    event.target.closest(".lesson-card")?.classList.add("image-missing");
  },
  true,
);
window.addEventListener("resize", () => {
  if (state.mode === "match") drawMatchLines();
});

render();
